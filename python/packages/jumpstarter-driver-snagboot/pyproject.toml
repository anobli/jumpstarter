[project]
name = "jumpstarter-driver-snagboot"
dynamic = ["version", "urls"]
description = "snagboot (snagrecover/snagflash) driver for Jumpstarter"
readme = "README.md"
license = "Apache-2.0"
authors = [
    { name = "Alexandre Bailon", email = "abailon@baylibre.com" }
]
requires-python = ">=3.11"
dependencies = [
    "anyio>=4.10.0",
    "click>=8.1.7.2",
    "jumpstarter",
    "jumpstarter-driver-opendal",
    "pyyaml>=6.0",
]

[project.entry-points."jumpstarter.drivers"]
Snagboot = "jumpstarter_driver_snagboot.driver:Snagboot"

[tool.hatch.version]
source = "vcs"
raw-options = { 'root' = '../../../'}

[tool.hatch.metadata.hooks.vcs.urls]
Homepage = "https://jumpstarter.dev"
source_archive = "https://github.com/jumpstarter-dev/repo/archive/{commit_hash}.zip"

[tool.pytest.ini_options]
addopts = "--cov --cov-report=html --cov-report=xml"
log_cli = true
log_cli_level = "INFO"
testpaths = ["jumpstarter_driver_snagboot"]
asyncio_mode = "auto"

[build-system]
requires = ["hatchling", "hatch-vcs", "hatch-pin-jumpstarter"]
build-backend = "hatchling.build"

[tool.hatch.build.hooks.pin_jumpstarter]
name = "pin_jumpstarter"

[dependency-groups]
dev = [
    "pytest-cov>=6.0.0",
    "pytest>=8.3.3",
]
